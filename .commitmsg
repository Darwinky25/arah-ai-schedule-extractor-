v2.1: logging, type hints, vectorized validation, overlap detection, CLI, tests

- Fix JSON NaN serialization errors (NaN to null)
- Replace all print() with structured logging module
- Add type hints to all public functions and classes
- Vectorize validator checks (no more iterrows)
- Add room time-overlap detection (replaces exact-match)
- Add lecturer conflict detection (new)
- Add multi-session deduplication (collapse redundant rows)
- Add artifact cleanup for rows with empty lecturer + empty room
- Extract magic numbers to named constants (MIN_DAY_MATCH_SCORE, etc.)
- Fix NaN string comparison bug in _smart_fill_forward
- Make end_time_cap configurable via constructor
- Add argparse CLI with --input-dir, --output-dir, --verbose
- Remove unused sys import from main.py
- Pin dependency versions in requirements.txt
- Rewrite compare_truth.py with key-based merge alignment
- Add 53 unit tests (pytest) covering core functions and validator
- Update README for v2.1 with new features and performance table
